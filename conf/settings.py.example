# Copy this file to `conf/settings.py` to put it into effect. It overrides the values defined
# in `waiverdb/config.py`.
SECRET_KEY = 'replace-me-with-something-random'
#DATABASE_URI = 'postgresql+psycopg2://dbuser:dbpassword@dbhost:dbport/dbname'
DATABASE_URI = 'sqlite:////var/tmp/waiverdb_db.sqlite'
JOURNAL_LOGGING = False
#SHOW_DB_URI = False
HOST= '0.0.0.0'
PORT = 5004
## Alternatively, you could use stomp to publish messages to a message bus.
#MESSAGE_PUBLISHER = 'stomp'
#STOMP_CONFIGS = {
#    'destination': 'topic://VirtualTopic.eng.waiverdb.waiver.new',
#    'connection': {
#        'host_and_ports': [
#            ('broker01', 61612),
#            ('broker02', 61612),
#        ],
#        'use_ssl': True,
#        'ssl_key_file': '/path/to/key/file',
#        'ssl_cert_file': '/path/to/cert/file',
#        'ssl_ca_certs': '/path/to/ca/certs',
#    },
#    # Alternatively, you can connect STOMP server with username and password.
#    'credentials': {
#        'username': 'username',
#        'password': 'password',
#    }
#}
